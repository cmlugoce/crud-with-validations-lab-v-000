<tbody>
<% @songs.each do |song| %>

 <tr>
    <%= song.title %>
    <%= song.released %>
    <%= song.artist_name %>
    <%= song.genre %>
  </tr>
<%= link_to 'Delete', song, method: :delete, data: { confirm: 'For real?' } %>
  <% end %>

</tbody>
